# Security

Report vulnerabilities privately to the maintainers (see the repository's contact) rather than in a
public issue. Never commit API keys: `.env` is git-ignored, and the userscript stores its server key
in Tampermonkey storage only.
